/**
 * Backend API types. This file is regenerated from the running backend's
 * OpenAPI spec via `npm run generate-types` (backend must be up at
 * http://localhost:8000). Until then, hand-maintained types live in each
 * service module. This stub keeps the path importable.
 */

export {}
