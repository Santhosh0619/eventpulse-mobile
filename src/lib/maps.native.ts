import RNMapView, { Marker as RNMarker } from 'react-native-maps'

/** Native (iOS/Android) maps, backed by react-native-maps. */
export const MapView = RNMapView
export const Marker = RNMarker
export const mapsAvailable: boolean = true
